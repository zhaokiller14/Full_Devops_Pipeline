[project]
name = "tp4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "prometheus-client>=0.19.0",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
